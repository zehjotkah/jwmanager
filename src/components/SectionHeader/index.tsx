import React from 'react'

// This component renders nothing but is properly exported
// so it can be referenced in the importMap
const SectionHeader: React.FC = () => null

export default SectionHeader
